formulas
dot product
u * v = u1 * v1 + u2 * v2 + u3 * v3 + …
“are these perpendicular?”
if dot product is 0
AKA orthogonal
orthogonal 2

length
|| v || = sqrt( x1^2 + x2^2 + x3^2 )
“how long is this vector”
“magnitude”

projection
proj_u(w) = 	(w * u) * u
/
u * u
“projecting onto a line”
“shadow of w onto v”

projection onto subspace
proj_w(w) = \big_sigma 	w * ui) ui
/
ui * ui
projection onto span(u,v,...)
“onto a plane / span “

distance = || w - proj_w(w) ||
shortest distance problems
distance from vector to plane / line

dim( W^⊥ ) = n - dim( W )
complement dimension questions
W^⊥ dimension 

rank + nullidity = number of columns

Ax = 0 <-- only trivial solutions

pivots = independent columns

no pivots = dependent

basis = independent + span 

"k independent vectors in R^n span k-dim subspace"

invertible = {  det != 0 
                rank = n 
                n pivots
                null space = 0 

det(A) = \l1 * \l2 * ... +  \ln
    whichever
    detemeninat mind you

trace(A) = \l1 + \l2 + \l3 + ... + \ln

Eigenvector definition
Av = \lv

characteristic equation
det(A - \LI) = 0 
it's that one thing where
1 - \l  2       3
4       4 - \l  5
6       7       8 - \l 

A = PDP^-1
